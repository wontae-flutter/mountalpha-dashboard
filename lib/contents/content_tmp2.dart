import "package:flutter/material.dart";

class Tmp2Content extends StatelessWidget {
  const Tmp2Content({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Tmp2"),
    );
  }
}
